using System;


namespace Arrays
{
    class ReverseArrayofIntegers
    {
        var n = int.Parse(Console.ReadLine());
        var arr = new int[n];
        for (int i = 0; i<n; i++)
			{
             arr[i] = int.Parse(Console.ReadLine);
			}


    }
}
